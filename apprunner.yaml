runtime: python3 
build:
  commands:
    pre-build:
      - cd py-backend
    build:        
      - pip install -r ./requirements.txt
run:
  runtime-version: 3.8.9
  command: uvicorn app.main:app --host 0.0.0.0 --port 8080